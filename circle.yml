machine:
  timezone:
      America/Los_Angeles
  java:
    version: openjdk8

  environment:
      SERVER_SOURCE: server
test:
  override:
    - cd $SERVER_SOURCE && mvn clean test
    - cd $SERVER_SOURCE && mvn jacoco:report
    - cd $SERVER_SOURCE &&  bash <(curl -s https://codecov.io/bash)